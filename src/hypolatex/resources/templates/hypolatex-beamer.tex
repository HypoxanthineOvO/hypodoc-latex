% Generated by Hypo-LaTeX. Do not edit directly.
\documentclass[aspectratio=$if(aspectratio)$$aspectratio$$else$169$endif$]{beamer}
% Legacy marker: \documentclass{beamer}
\usepackage{hypolatex-beamer}

$if(title)$\HypoSetMetadata{title}{$title$}
\title{$title$}
$endif$$if(subtitle)$\HypoSetMetadata{subtitle}{$subtitle$}
\subtitle{$subtitle$}
$endif$$if(author)$\HypoSetMetadata{author}{$for(author)$$author$$sep$\\$endfor$}
\author{$for(author)$$author$$sep$\\$endfor$}
$endif$$if(organization)$\HypoSetMetadata{organization}{$organization$}
$endif$$if(course)$\HypoSetMetadata{course}{$course$}
$endif$$if(date)$\HypoSetMetadata{date}{$date$}
\date{$date$}
$endif$$if(version)$\HypoSetMetadata{version}{$version$}
$endif$$if(logo)$\HypoSetMetadata{logo}{$logo$}
$endif$$if(palette)$\HypoSetMetadata{palette}{$palette$}
$endif$$if(footline)$\HypoSetMetadata{footline}{$footline$}
$endif$$if(icon)$\HypoSetMetadata{icon}{$icon$}
$endif$$if(abstract)$\HypoSetMetadata{abstract}{$abstract$}
$endif$$if(theme)$\HypoSetMetadata{theme}{$theme$}
$endif$$if(layout)$\HypoSetMetadata{layout}{$layout$}
$endif$
\begin{document}

$if(title)$
\frame{\titlepage}
$endif$

$body$
\end{document}
